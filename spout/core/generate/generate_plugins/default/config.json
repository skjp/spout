{
    "schema": 1,
    "description": "Generate one or more strings of text based on a description and example, avoiding duplicates with the already generated items",
    "execution_settings": {
        "default": {
      "max_tokens": 4096,
      "temperature": 0.1,
      "top_p": 0.3,
      "presence_penalty": 0.0,
      "frequency_penalty": 0.0,
      "response_format": {"type": "json_object"}
        }
    },
    "input_variables": 
       [
        {
          "name": "description",
          "description": "description of the list to generate, example of the items in the list, batch size, already generated items",
          "defaultValue": ""
        },
        {
          "name": "example",
          "description": "example of the items in the list",
          "defaultValue": ""
        },
        {
          "name": "batch_size",
          "description": "batch size",
          "defaultValue": "1"
        },
        {
          "name": "already_gen",
          "description": "already generated items",
          "defaultValue": ""
        }

      ]
    
  }
  